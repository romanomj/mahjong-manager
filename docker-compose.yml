version: '3.8'

services:
  mahjong-manager:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - ./music:/app/public/music
      - ./server/mahjong.db:/app/mahjong.db
    restart: unless-stopped
    environment:
      - NODE_ENV=production
