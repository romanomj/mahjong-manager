version: '3.8'

services:
  mahjong-manager:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - ./music:/app/public/music
      - ./server/data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
